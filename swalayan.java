import java.io.*;

public class swalayan {
	public static void main (String[] args)IOException{
		BufferedReader br =new BufferedReader(new InputStreamReader(System.in));
		int nn,beras,gula,minyak,up;
		
		System.out.println("No.Nota :                          ");
		nn =Integer.parseInt(br.readLine());
		System.out.println("Beras : (kg)                       ");
		beras =Integer.parseInt(br.readLine());
		System.out.println("Gula : (kg)                        ");
		gula =Integer.parseInt(br.readLine());
		System.out.println("Minyak (kg)                        ");
		minyak =Integer.parseInt(br.readLine());
		
		int total1 =beras*1
	}
}